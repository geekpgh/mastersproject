@model BrewersBuddy.Models.BatchNote

@{
    ViewBag.Title = "Edit";
}

<h2>EditNote</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Note</legend>

        @Html.HiddenFor(model => model.NoteId)
        @Html.HiddenFor(model => model.AuthorId)
        @Html.HiddenFor(model => model.AuthorDate)
        @Html.HiddenFor(model => model.BatchId)

        <div class="editor-label">
            @Html.LabelFor(model => model.Title)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Title)
            @Html.ValidationMessageFor(model => model.Title)
        </div>
        <div class="editor-label">
            @Html.LabelFor(model => model.Text)
        </div>
        <div class="editor-field">
            @Html.TextArea("Text", new { rows=10, columns=40 })
            @Html.ValidationMessageFor(model => model.Text)
        </div>

        <p>
            <input class="btn btn-success" type="submit" value="Save" />
            @Html.ActionLink("Cancel", "Details", "Batch", new { id = Model.BatchId }, new { @class = "btn btn-danger" })
        </p>
    </fieldset>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
